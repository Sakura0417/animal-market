# 《动物集市》开发任务拆解 · 核心玩法优先（DEV-PLAN）

依据：技术选型文档（Cocos Creator 3.8 + TS，四层架构）/ PRD V1.3 / 高保真原型 V4.1。
本轮范围（用户指定）：**只做核心玩法闭环，进入页面即进入对局**。首页、图鉴、签到、农场升级、每日挑战、每日签到全部延后。

## 范围决策

| 模块 | 本轮 | 说明 |
|---|---|---|
| 对局核心玩法（散点/遮挡/碰撞弹回/生命/清空通关） | ✅ | 全量移植 game.js V4.1 |
| 救援道具（吊车×4/翻转×2/洗牌×2）+ 广告续命 | ✅ | 广告用 Mock（3 秒演示层） |
| 车位系统（3 开 + 2 广告解锁）+ 订单/发车/补车 | ✅ | |
| 碰撞/通关全特效（粒子/飘字/震屏/心碎/彩带/面板弹跳） | ✅ | Graphics + Tween 重做 |
| 首页/图鉴/农场升级/每日挑战/签到 | ❌ 延后 | 资产已留位（P6） |
| 结算独立页 | ❌ 延后 | 本轮用对局内胜利面板（星级=剩余生命） |
| 真实微信广告 API | ❌ 延后 | WxBridge 已留桩，P5 接入 |

## 阶段拆解（P0-P4 本轮交付，P5 用户手动，P6 后续）

> 勾选回填：2026-09-05 23:52:00（交接后接手开发核对实际进度补记，基线见交接文档第 3 节）

- [x] **P0 工程脚手架** — package.json / tsconfig / .gitignore / README / Game.scene（最小场景）（README 与 Game.scene 已于 2026-09-05 补齐）
- [x] **P1 core 逻辑层**（对应技术文档 M1）— `assets/scripts/core/`：Types / Constants / LevelCfg / Board / GameSession，**零 cc 依赖**，逐行对齐 game.js V4.1（散点生成、走廊遮挡、订单偏置、碰撞弹回、救援链、死局判定）（同种子对拍 12/12 关一致）
- [x] **P2 模拟器回归**（M1 验收）— `tools/sim/BalanceSim.ts` 复用 core 跑贪心机器人；与原型 `_sim-trace.js` **同种子对拍**，L1-L12 输出必须一致才算移植零失真；曲线达标线：L1-2 一审≥95%、恢复后全程≥95%、0 卡死（2026-09-05 实测：L1–L12×400 局全达标、0 卡死；verify 100 全绿）
- [x] **P3 表现层**（对应 M2，代码构建 UI）— `assets/scripts/view/`：GameView（总控，移植 game.js 控制流）/ AnimalNode / TruckNode / FenceView / ParkView / HudView / FxService / Panels / Draw2D（矢量绘制，替代原型 SVG 占位）（GameView.ts 已于 2026-09-05 补齐；视觉表现待 P5 预览核对）
- [x] **P4 服务与平台桩** — SaveService（关卡进度/钱包，sys.localStorage）/ AdService（Mock 广告层 + 点位频控）/ PlatformAdapter（dev mock / wx 双实现，唯一出现 wx.* 的层）
- [ ] **P5 编辑器接入（用户手动，约 30 分钟）** — Cocos Creator 3.8.x 打开工程 → 浏览器预览 → 构建发布微信小游戏（填 AppID）→ 微信开发者工具调试。见 README「快速开始」（Game.scene + GameView 已就位；2026-09-06 01:35 web-mobile 构建全绿 + 浏览器冒烟通过核心链路，剩 12 关逐关人工通关 A3、特效对拍 A5、微信构建 A4）
- [ ] **P6 后续迭代** — 真广告接入与合规频控 / 分包（主包≤4M）/ 美术替换（Draw2D 占位 → PNG 图集三态动物）/ hub 模块回归（首页/图鉴/签到/农场/挑战）/ 真机性能红线（中端机 ≥50fps）

## 关键工程决策（为什么这么做）

1. **core 层禁止 import 'cc'**：难度曲线是本作最贵资产（原型 6400 局验证）。保持纯 TS，BalanceSim 才能在 CI 里持续守护每次改动。
2. **UI 全部代码构建**（Graphics/Tween，不用编辑器拼预制体）：美术资产未产出前，先跑通玩法闭环；scene 只挂一个 GameView 组件，加载失败风险最小。
3. **视觉随机量（rot 倾斜）放 view 层**：原型动物 rot 消耗随机数在模拟器中不存在，为保种子对拍零失真，rot 由 view 生成。
4. **BalanceSim 放 tools/ 不放 assets/**：避免未引用代码打进小游戏包体。

## 对局流程（V4.1 规则，移植基准）

点击动物 → 走廊判定：未被挡 → 弧线飞入围栏（满 7 格 → 挤爆失败链）；被挡 → 冲撞弹回原位 + 扣 1 生命 + 全套特效（3 命耗尽 → 广告复活失败面板）。围栏凑齐卡车订单 → 逐只装车 → 发车收钱 → 车位立即补新车。死单自动换车。全场动物+围栏清空 → 通关。互锁时走救援链：吊车（免扣血吊走）→ 翻转朝向 → 洗牌重排。
