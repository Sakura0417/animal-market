# 《动物集市》PRD V2.0 对齐梳理（2026-09-06 18:02）

> 依据：`animal-market-prd/animal-market-prd.html`（PRD **V2.0**，2026-09-06 17:54 更新，75,910 B；旧版 65,381 B）
> 范围：重梳 2026-09-06 完成的全部工作（需求理解 / 设计方案 / 任务拆解 / 已写代码）
> 方法：PRD 2.0 全文解析 + 19 个 TS 源码逐项核对 + `BalanceSim` 实跑复核

---

## 0. 结论速览（先看这个）

| # | 结论 | 严重度 |
|---|---|---|
| 1 | **我上一轮的核心结论「否决网格化」已被 PRD 2.0 推翻，整条作废**。依据是 PRD 1.3 的 `level.layout = 非网格；v1.0 不做网格模式`——该条款在 V2.0 中已被删除并反转 | 🔴 高 |
| 2 | **`docs/grid-layout-and-collision-analysis.md` 与 PRD 2.0 高度一致，应从「待评审建议」升格为「实现参考蓝本」**（6×10 / 格距 47.7 / 精灵 43.5 / 60 格 / 57% 填充率 / 方案 A·B·C / 生成器 5 步 / JSON 格式 / 编辑器 Phase A·B·C / 不迁物理——全部被采纳） | 🟢 正向 |
| 3 | **优先级规则反转**：上一轮确立的「原型 V4.1 > PRD」作废。PRD 2.0 明确"内嵌原型为散点时代 V2 历史演示，机制描述一律以 V2.0 正文为准" → 改为 **PRD 2.0 > 原型** | 🔴 高 |
| 4 | **最大缺口 = 网格化本身**（盘面 / 遮挡 / 生成器 / 洗牌 / 土路 / 命中 / 接触距离 七处连带），约 4–5 人日 | 🔴 高 |
| 5 | 已写代码**约 70% 可保留**——生命值、清空判定、双失败态、复活、不迁物理、订单系统、三道具、互锁救援全部与 PRD 2.0 对齐 | 🟢 正向 |
| 6 | **种子对拍基准必然失效**：网格化改变随机流，`sim:verify` 与原型 `_sim-trace.js` 的对拍会全部失败。PRD 2.0 未说明新基准 → 需裁决（见 D4） | 🟡 中 |

---

## 1. PRD 2.0 相对旧版的关键变更点

### 1.1 官方「版本修订记录（V1.1 → V2.0）」8 项

| # | 变更项 | V1.1（散点时代） | **V2.0（现行）** | 变更理由（PRD 原文） |
|---|---|---|---|---|
| 1 | **牧场布局** | 拒绝采样散点 + 走廊遮挡（射线 + 容差） | **6×10 等尺寸网格、四朝向、紧密排列 + 相邻格遮挡（O(1) 占用图查表）** | 对齐猪了个猪/挪车品类语义，消除"眼前是空的却撞了"的反直觉；布局可编辑、可序列化 |
| 2 | **被挡动物交互** | 置灰锁点，不可点击 | **可点击，碰撞弹回扣 1 生命（3 心），耗尽失败** | 生命值玩法（已落地 V4.1）；"硬拿有代价"成为次生风险决策与复活广告点 |
| 3 | **通关判定** | 完成 N 张订单（targetOrders） | **清空判定：牧场 + 围栏全部清空** | 清场目标更直觉；星级改按剩余生命结算 |
| 4 | **失败态** | 单一：围栏挤爆 | **双失败态：围栏挤爆 + 生命耗尽，各挂独立转化面板** | 复活点位补齐（激励视频第 10 点位） |
| 5 | **碰撞实现** | （未定论） | **明确不迁移 Cocos 物理引擎，保持解析式规则判定** | 确定性/种子对拍、包体、回合制语义三重论证 |
| 6 | **关卡数据** | 生成参数（密度/朝向偏置等） | **关卡 JSON schema + 约束式网格生成器 + 编辑器三阶段路线** | 网格使"格子即数据"，为编辑器与数值即时验证铺路 |
| 7 | **难度数据** | 1600 局（一审 24% / 恢复 99.9%） | **4800 局实测基线**（一审 L1-2 100% → L12 55.3%；恢复后 100%；0 卡死；对拍 12/12） | 生命值 + 清空判定机制下的最新模拟数据；**网格化落地后需重建** |
| 8 | **引擎版本** | Cocos Creator 3.x | **Cocos Creator 3.8.8（锁定）** | 与工程 package.json 及实际安装引擎一致 |

### 1.2 修订记录未列、但正文已变的额外 9 项（我核对出的）

| # | 变更 | V2.0 新要求 |
|---|---|---|
| 9 | 广告点位 9 → **10** | 新增「失败·复活」（2/局，清空判定的核心兜底） |
| 10 | **星级系统** | 明确"星级按剩余生命结算"；结算分享卡含"星级"字段 |
| 11 | 每日挑战计分口径 | 完成订单数 + **剩余生命** + 剩余围栏格数 |
| 12 | 生命耗尽表现 | 画面定格 + **全场动物表情黯淡 0.5 秒** |
| 13 | 洗牌语义 | **网格版为「空格间重排」**（朝向不变） |
| 14 | 字段规则 | `animal.col/row`（int 0-5 / 0-9）**取代**浮点 x/yu；新增 `level.grid`(6×10) / `level.hp`(3) / `level.winCondition`(清空判定) |
| 15 | 难度目标 | 前 2 关 **>95%**（原 >90%）；第 3 关起 **85-95%**（原 70%）；中后期 **60% 上下** |
| 16 | 死局术语 | 扩展为两大失败态 + "围栏剩货凑不齐任何订单"的终局判定 |
| 17 | **原型地位** | 内嵌原型降级为"散点时代 V2 历史演示"；**机制一律以 V2.0 正文为准**；网格机制的交互验证以 Cocos 工程预览为准 |

---

## 2. 现有工作 × PRD 2.0 差异清单

### A. 已对齐 —— ✅ 保留（约 70% 已写代码）

| 项 | PRD 2.0 要求 | 现状 | 位置 |
|---|---|---|---|
| 生命值 3 心 | `level.hp = 3`，碰撞扣 1 | ✅ `HP_MAX = 3` | `core/Constants.ts:7` |
| 碰撞弹回扣心 | 可点击 → 冲向遮挡者 → 红闪/粒子/飘字/震屏/心碎 → 扣 1 心 → 弹回 | ✅ 完整实现（约 380px/s 真实行进） | `view/AnimalNode.ts:88` `view/GameView.ts:329-388` |
| 被挡动物不置灰 | 明确"不置灰、不锁点" | ✅ 本就未置灰 | `view/PenView.ts:85`（仅 craneMode 时 setCrane） |
| 清空通关判定 | 牧场 + 围栏全部清空 | ✅ `herdLeft===0 && fence.length===0` | `core/GameSession.ts:56` |
| 双失败态 | 挤爆 + 生命耗尽，各挂独立面板 | ✅ `failFence` + `failHp` | `view/GameView.ts:605/640` |
| 广告复活 | 回满 3 心、原地续局、营收保留 | ✅ `revive()` | `core/GameSession.ts:296` |
| 不迁物理引擎 | 解析式规则判定 | ✅ 无任何 cc.physics 依赖 | 全 core 层 |
| 围栏 7 格 / 车位 5（3 开 2 锁） | — | ✅ `FENCE_MAX=7` `SLOT_TOTAL=5` `SLOT_OPEN=3` | `core/Constants.ts:3-5` |
| 订单可完成性校验 + 75% 偏向采样 + 装车链调和 + 死单换车 | — | ✅ 全部实现 | `core/GameSession.ts:87-146/445-467` |
| 三道具（吊车/翻转/洗牌）+ 互锁自动送吊车 + 8s 超时续时 | — | ✅ `craneTick` | `core/GameSession.ts:250-274` `view/GameView.ts:95-103` |
| 核心 8 个激励视频点位 | 吊车/车位/翻转/洗牌/放生/清仓/复活/双倍 | ✅ 8 处 `ad.show` | `view/GameView.ts:545/565/577/592/608/614/645/687` |
| 数值区间 | animalCount 10-34 / obstacles 0-4 / types 3-6 / hp 3 | ✅ 全部吻合（L1=10、L20+=34） | `core/LevelCfg.ts` |
| 引擎 3.8.8 | 锁版本 | ✅ | `package.json` |
| 4800 局基线 | L1-2 100% / L4 98.8% / L8 75.0% / L12 55.3% / 挤爆 0→45.3% / 0 卡死 | ✅ **本次实跑复核吻合**（L4 99.5%、L8 74.5%、L12 53.8%、挤爆 0→43.8%、0 卡死） | `npm run sim` 实测 |

### B. 需修改 —— ⚠️

| # | 项 | PRD 2.0 | 现状 | 位置 | 处理 |
|---|---|---|---|---|---|
| **B1** | **盘面布局** | 6×10 网格、动物占整格、紧密排列 | 散点 `scatter` + 浮点 `x/yu` | `core/Types.ts:9-17`（Animal.x/yu）、`core/Board.ts:89-107`、`core/Constants.ts:11-16`（R/TOL/AHEAD/MIN_D/PAD） | 🔴 **修改（核心）** |
| **B2** | **遮挡判定** | 相邻格遮挡、O(1) 占用图、边界格朝外视为空 | 走廊遮挡（`blockerOf` 射线 + TOL，无限远） | `core/Board.ts:36-50` | 🔴 **修改（核心）** |
| **B3** | **生成器** | 约束式填充（空网格→岩石→动物→朝向→保底修复） | 拒绝采样 + 40 次整体重摇 | `core/GameSession.ts:60-81`、`core/Board.ts:89-107` | 🔴 **修改** |
| **B4** | **洗牌** | 网格版「空格间重排」（朝向不变） | `scatter` 重排 → **会直接把网格洗回散点** | `core/GameSession.ts:257-266` | 🔴 **修改**（B1 硬性连带，不做即破功） |
| **B5** | 翻转 | 打散相邻遮挡结构 | `randDir` 全体随机（语义不变） | `core/GameSession.ts:250-255` | 🟡 保留实现，仅回归验证 |
| **B6** | **吊车次数** | 每局 **3 次**（首次免费 + 激励 2 次） | `CRANE_MAX = 4`（原型亦 4） | `core/Constants.ts:6` | 🟡 **待裁决** → 见 §4-D1 |
| **B7** | 土路行走路径 | 动物按朝向走出"网格"沿土路入栏 | `roadWaypoints` 基于散点 `(x, yu)` 换算 | `view/PenView.ts:122-155` | 🟠 **修改**（B1 连带） |
| **B8** | 命中判定 | 点格即命中 | 最近邻圆匹配（阈值 27px），实测误点率 0.3–2.0% | `view/PenView.ts:59-70` | 🟠 **修改**（B1 连带，顺带消解误点） |
| **B9** | 碰撞接触距离 | 格距 − 精灵尺寸 | 散点投影距离 − 2×R×0.95 | `view/GameView.ts:340-355` | 🟠 **修改**（且需裁决 → §4-D2） |
| **B10** | 难度基线 | 网格化后重建 | 当前为散点基线 | `tools/sim/BalanceSim.ts` | 🟠 **修改**（重跑 + 重新归档） |

### C. 需新增实现 —— 🆕

| # | 项 | PRD 出处 | 现状 | 处理 |
|---|---|---|---|---|
| **C1** | Grid 占用图 + `Animal.col/row` 字段 | 5.1 字段规则 | 无 | 🔴 **新增**（B1 的地基，先于一切） |
| **C2** | **关卡 JSON schema（Phase A）** | 5.1 网格布局规范 + **5.8 v1.0 范围明确包含** | 无 | 🔴 **新增**。⚠️ 我上一轮把它推 P6 是**错的**——PRD 5.8 把「关卡 JSON 与编辑器 Phase A」列进 v1.0 范围 |
| **C3** | **星级系统**（按剩余生命结算） | 术语表 / 5.4 / 5.7 分享卡 | 无（`Panels.win` 只有金币与售出统计） | 🟠 **新增** |
| **C4** | 生命耗尽「全场动物表情黯淡 0.5s」 | 5.4 | 无 | 🟢 新增（小） |
| **C5** | 「失败·复活」频次上限 2/局 | 5.5 表格 | `revive()` 无次数限制 | 🟢 新增（小） |
| C6 | 编辑器 Phase B / C | 5.1 | 无 | ⚪ P6（PRD 只把 Phase A 放入 v1.0） |
| C7 | 每日挑战计分加「剩余生命」 | 5.6 | 未做 | ⚪ P6，记入口径 |
| C8 | 签到翻倍 / 图鉴加速 2 个广告点位 | 5.5（凑满 10 个） | 无 | ⚪ P6（随图鉴/签到） |
| C9 | 数据埋点（07 章 6 类事件） | 07 | `SaveService` 无任何埋点 | ⚪ 待确认 → §4-D7 |

### D. 需删除 / 废弃 —— ❌

| # | 项 | 原因 |
|---|---|---|
| **D1** | **我上一轮的评审结论「否决网格化」** | 依据是 PRD 1.3 `非网格；v1.0 不做网格模式`——该条款已被 V2.0 删除并反转。**整条结论作废** |
| **D2** | 上一轮确立的「需求优先级：原型 V4.1 > PRD」 | PRD 2.0 明确原型是"散点时代 V2 历史演示、机制以正文为准"。**反转为 PRD 2.0 > 原型** |
| **D3** | 上一轮的执行计划（阶段 0-4 上线冲刺） | 与 PRD 2.0 v1.0 范围冲突（缺网格化与关卡 JSON）。**由本文 §3 计划替换** |
| **D4** | `docs/review-and-plan-20260906.md` 的 §4 方案与 §5 拍板章节 | 已被 PRD 2.0 推翻，需标注作废（保留 §2 评审数据作为历史） |
| **D5** | 常量 `R / TOL / AHEAD / MIN_D / PAD / ASPECT` | 网格化后由 `GRID_COLS / GRID_ROWS / CELL_PITCH` 取代 |
| **D6** | 4800 局散点基线的「验收标准」地位 | 仍归档备查，但**不再作为验收线**；网格化后重建 |
| **D7** | 上一轮拍板的「不做遮挡可视化」 | 结论**仍然成立**（PRD 2.0 亦规定"不置灰、可点击"），但**理由变了**——不是"原型实证废弃"，而是 PRD 2.0 明文规定 |

### E. 我上一轮评审中仍然成立的部分 —— ✅ 保留

| 项 | 状态 |
|---|---|
| 不迁移物理引擎 | ✅ PRD 2.0 采纳为定论，与我的结论一致 |
| F2 事实错误（package.json 已修） | ✅ 与版本无关，仍成立 |
| F4 事实错误（Game.scene 存在、冒烟已完成） | ✅ 与版本无关，仍成立；且"编辑器当前激活未保存默认场景"仍是真实状态风险 |
| P2 `blockerOf` 返回非最近者（L20+ 44%） | 🟡 散点期是真 bug，但**网格化后自然消解** → 处理方式由「单独修」改为「随 B1/B2 一并解决」，省一次改动 |
| P4 命中歧义 2% | 🟡 随 B8 消解 |
| P6 数值/视觉耦合 | 🟡 随 B1 消解 |
| **风险提示：相邻遮挡缩短连锁解锁长度** | ⚠️ **仍然成立，建议保留为验收观察项**。走廊移走一只解锁一整串，相邻只解锁身后一只。PRD 2.0 亦承认"网格化落地后需重建基线"，需靠密度/岩石/高关卡反向偏置补偿难度——实现后必须用 BalanceSim 量化验证 |

### F. `grid-layout-and-collision-analysis.md` 重新定位

| 项 | 结论 |
|---|---|
| 总体 | 🟢 **从「待评审建议」升格为「实现参考蓝本」**——其 6×10 / 格距 47.7 / 精灵 43.5 / 60 格 / 57% / 方案 A·B·C / 生成器 5 步 / JSON 格式 / 编辑器 Phase A·B·C / 不迁物理，全部与 PRD 2.0 一致 |
| 需修正处 1 | §2.1「Game.scene 仍未创建」——事实错误（磁盘存在 `a3f8d2e1…`） |
| 需修正处 2 | F4「19 模块未验证过运行」——事实错误（冒烟与 L1 通关早已完成） |
| 需修正处 3 | F2「package.json 脚本失效」——已修（DEVLOG 09-05 23:44） |
| 需修正处 4 | §3.3「连锁解锁有且更强」——与实际相反，且未评估难度补偿方案 |

---

## 3. 调整后的执行计划与优先级

> 排期依据：PRD 2.0 §5.8 周期表——W1-2 网格布局 + 相邻遮挡 + 生命值 + 装车；W3 关卡数据结构与网格生成器 + 编辑器 Phase A。

| 优先级 | 阶段 | 内容 | 量级 | 验收 |
|---|---|---|---|---|
| **P1** | core 网格化 | C1 Grid 占用图 + `Animal.col/row` → B2 相邻遮挡（O(1)）→ B3 约束式生成器 → B4 洗牌改空格间重排 → B5 翻转回归 | 2–2.5 天 | core 可 Node 直跑；新生成器解锁数 ≥6 保底生效；⚠️ 旧 `sim:verify` 对拍**必然失败**（见 §4-D4） |
| **P2** | 关卡 JSON Phase A | 冻结 schema（生成器输出即此格式），定义 TS 类型 | 0.5 天 | `JSON.stringify(gen.output())` 结构符合 PRD 示例 |
| **P3** | view 适配 | B7 `roadWaypoints` 改格边界 → B8 `onTap` 点格命中 → B9 接触距离 = 格距 − 精灵 → PenView 格→像素映射 | 1.5 天 | 预览内四朝向行走路径正确；点击零误判；碰撞动画可见（见 §4-D2） |
| **P4** | 难度重建 | BalanceSim 重跑 4800 局，按 PRD 目标调参（密度 / 岩石 / 高关卡反向偏置） | 1 天 | L1-2 ≥95%、L3-5 ≥85%、L6-12 55-75%、恢复后 100%、**0 卡死** |
| **P5** | 补齐小项 | C3 星级（按剩余生命）→ C4 生命耗尽黯淡 0.5s → C5 复活上限 2/局 → B6 吊车次数 | 0.5 天 | 三项可预览验证 |
| **P6** | 上线收尾 | A3 逐关验收 + A5 特效对拍 → A4 微信构建真机 → A7 真实广告 →（编辑器 Phase B/C） | — | 同 DEV-PLAN |
| **P0**（先做） | 文档与规则纠正 | 作废 review-and-plan v2 结论、更新 DEVLOG/MEMORY 的优先级规则、冻结散点基线为历史 | 0.5 天 | 本文落盘 + 规则同步 |

**排序理由**：C1（Grid 占用图）是 B2/B3/B4/B7/B8/B9 六项的公共地基，必须最先做；P2 的 schema 成本≈0 且 PRD 明确列入 v1.0 范围，紧随 P1；P4 难度重建必须在 P1-P3 之后才有意义（PRD 自己也是这么说的）。

---

## 4. 需求歧义与信息缺失（明确指出，不自行猜测跳过）

| # | 歧义 / 缺失 | 冲突内容 | **我的默认处理方式** |
|---|---|---|---|
| **D1** | **吊车每局次数** | PRD 2.0 §5.4「每局 **3 次**（首次免费 + 激励视频 2 次）」vs 原型 `game.js:40` 与 Cocos `Constants.ts:6` 均为 **4** | 按你的指令"以 PRD 2.0 为唯一标准"→ **默认改为 3**。⚠️ 但这是**唯一一处 PRD 与原型/实现三方不一致**，若保留 4 有理由（如难度实测需要），请明示 |
| **D2** | **碰撞冲刺距离过短** | PRD「接触距离 = 格距 − 精灵尺寸」= 47.7 − 43.5 = **4.2px**；按 380px/s 仅 **11ms**，动画肉眼不可见——按字面实现会得到一个几乎看不见的碰撞 | PRD 未规定最小值。**默认实现为「位移 = 格距 × 0.45 ≈ 21px」**（冲到轻微重叠，撞击感明显），实现后用预览调手感。这是"按字面实现会失效"的实测结论，不是我的偏好 |
| **D3** | **难度目标内部不一致** | §5.1 正文「第 3 关起 85-95%」；但同节实测基线 L4 = **98.8%**（超出该区间）；§6 章又写「通过率目标曲线 55-75%」 | **默认以 5.1 实测基线表为准**：L1-2 100%、L4 98.8%、L8 75%、L12 55.3%；验收线取 L1-2 ≥95%、L3-5 ≥85%、L6-12 55-75%、恢复后 100%、0 卡死。若应为别的口径请指出 |
| **D4** | **种子对拍基准失效（验收标准变更）** | 网格化必然改变随机流（生成器从 `scatter` 改为约束式填充），`sim:verify` 与原型 `_sim-trace.js` 的对拍**会全部失败**。PRD 2.0 未说明新基准 | **默认按 grid 文档 §5.2 建议**：对拍对象从「原型 `_sim-trace.js`」切换为「**core 自身跨版本对拍**」（固化一批种子 + 输出哈希作为新基准），4800 局散点曲线归档为历史基线。**这是验收标准的实质变更，需要你确认** |
| **D5** | **"紧密排列"与视觉尺寸矛盾** | PRD「动物精灵 ≈ 43.5」+「零视觉回归」，但格距 47.7 → 格间空隙 4.2px，"紧密排列"其实不紧密 | **默认保持 43.5**（PRD 明确"零视觉回归"优先），空隙用格线/格底影在视觉上弱化 |
| **D6** | **编辑器 Phase A 交付物形态** | PRD 只说"schema 随网格化一并冻结（成本≈0，生成器输出即此格式）"，未明确是否要文件读写 UI | **默认 Phase A = 只定 TS 类型 + 生成器可直接序列化输出该结构，不做文件读写 UI**（符合"成本≈0"表述） |
| **D7** | **数据埋点未规划** | PRD 07 章有 6 类事件完整埋点表；当前 `SaveService` 无任何埋点实现。而北极星指标「人均激励视频观看次数」**完全依赖埋点** | **默认：列为上线前必须**（否则无法验证成功标准），但排在 P6 之后、提审之前。⚠️ 请确认是否已有规划 |
| **D8** | **相邻遮挡下的难度补偿参数** | PRD 说"高关卡反向偏置制造链条"，但未给具体参数；相邻遮挡解锁更宽松（实测风险：初始可动数偏多、难度可能过松） | **默认：按 PRD 生成器规则实现，用 BalanceSim 扫参确定**（这正是 PRD 说的"网格化落地后需重建基线"）。实现后若一审通过率整体偏高，优先提密度/岩石数而非改判定 |

---

## 5. 附：本次核对的取证方式

- **PRD 2.0 全文解析**：标题骨架 + 正文全文（33,408 字符），重点核对 §5.1 网格布局规范 / 碰撞引擎决策 / 字段规则 / 难度曲线、§5.4 失败态、§5.5 点位矩阵、§5.8 v1.0 范围、§6 版本修订记录
- **源码核对**：`core/Types.ts`(坐标字段) / `core/Constants.ts`(CRANE_MAX=4) / `core/Board.ts`(blockerOf/scatter) / `core/GameSession.ts`(generate/useShuffle/useFlip/revive/isWin) / `view/PenView.ts`(onTap/roadWaypoints) / `view/GameView.ts`(8 处 ad.show / 碰撞分支) / `view/Panels.ts`(win 无星级) / `services/AdService.ts`
- **实跑复核**：`npm run sim`（L1-L12 各 400 局）——L1-2 100%、L4 99.5%、L8 74.5%、L12 53.8%、挤爆 0→43.8%、0 卡死，与 PRD 2.0 的 4800 局基线吻合
- **交叉核对**：`animal-market-prototype/game.js:40`（CRANE_MAX=4）、`docs/grid-layout-and-collision-analysis.md`（与 PRD 2.0 一致性比对）
